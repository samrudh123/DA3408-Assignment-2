apiVersion: v1
kind: Service
metadata:
  name: spam-api
  labels:
    app: spam-api
spec:
  type: NodePort
  selector:
    app: spam-api
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30080
